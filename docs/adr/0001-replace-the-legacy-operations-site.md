# Replace the legacy operations site

The product will replace the provider's existing operations site rather than integrate with it because its source code and data are unavailable, it retains less than a week of history, and depending on browser automation would leave core operations coupled to an uncontrolled system. Import, dispatch, and driver completion will be validated incrementally before a firm cutover, with the old site retained temporarily as a fallback and historical reference.
